function nametag_get_text(p: player) :: texts:

	# Display name
	set {_dp::1} to nametag_dp_admin({_p})
	set {_dp::2} to ""
	set {_dp::3} to nametag_dp_rank({_p})
	set {_dp::4} to nametag_dp_name({_p})
	set {_dp::5} to ""

	# Visszaküldjük
	return colored join {_dp::*} with nl